import { createSlice } from "@reduxjs/toolkit";

const initialState = {
   
}
export const singInSlice = createSlice({
    name: "signIn",
    initialState,
    reducers: {

    }
})

export const {  } = singInSlice.actions
export default singInSlice.reducer